# Восемь недель с криптой — версия v3, многостраничная

Ветка `v3`. **Никуда не задеплоена**: прод (8weekscrypto.com и GitHub Pages)
собирается из `main`, и эта ветка его не затрагивает.

Четыре страницы вместо одной. Прежний лендинг курса стал одной из них, вокруг
него выросли страница об эксперте, консалтинг для бизнеса и контакты.

## Страницы

| Адрес | Файл | Что это |
|---|---|---|
| `/` | `index.html` | Эксперт, развилка двух направлений |
| `/personal` | `personal.html` | Прежний лендинг курса + блок «кому подходит» |
| `/business` | `business.html` | Консалтинг для бизнеса |
| `/contacts` | `contacts.html` | Четыре сценария обращения |
| `/privacy` | `privacy.html` | Политика обработки данных |

Адреса без расширения. Railway и GitHub Pages такие отдают сами, локальный
`serve.py` повторяет это поведение — без него половина меню давала бы 404.

## Посмотреть локально

```
python3 serve.py     # http://localhost:4175/
```

## Три решения, о которых стоит знать

**Форм нет ни на одной странице.** ТЗ их просит, но форма требует приёмника
заявок, а вместе с ним согласия на обработку данных и хранения переписки —
всё то, что из проекта сознательно убрали. Вместо формы каждый сценарий
на `/contacts` открывает Telegram с уже набранным сообщением; у B2B-сценария
в тексте сразу поля: компания, роль, задача, что пробовали, сроки, нужен ли
только анализ. Результат тот же, но данные не проходят через сайт.

**Ни один непроверенный факт не выдуман.** Сорок четыре места помечены
`[ПОДТВЕРДИТЬ]` и подсвечены на странице классом `.fill`: названия агентств
и годы, роль в венчурной студии, стартапы, TON-проекты, все кейсы, все
отзывы, имя и часы менеджера, почта, компании специалистов. Выложить
страницу с рыбой случайно не получится.

**Портрет всё ещё 3D-рендер**, а не фотография. Это блокер и для главной,
и для `/personal`.

## Как устроены стили

`pages.css` подключается между `styles.css` и `responsive.css` и описывает
только те блоки, которых нет на странице курса: первый экран внутренних
страниц, развилку направлений, этапы, форматы, кейсы, отзывы, сценарии
и фильтр «подойдёт / не подойдёт».

Новых цветов и пропорций в нём нет — всё на тех же токенах. Адаптация новых
блоков живёт там же, а не в `responsive.css`: смешивать в одном файле две
разные раскладки — верный способ сломать одну, правя другую.

FAQ на главной построен на разметке недель программы: тот же аккордеон,
тот же скрипт, тот же вид.

## Что осталось за рамками первой версии

Аналитика (раздел 10 ТЗ) не подключена: счётчик потребует переписать
раздел 5 политики. Отдельная страница агентства, блог, мультиязычность
и CRM — это P2/P3.

---

# Предыдущая редакция README (версия v2, ветка `v2`)

Параллельная версия сайта, собранная по ТЗ на усиление доверия и юридическую
подготовку к приёму оплат. Дизайн-язык, сетка, типографика и анимации те же,
что в основной версии: менялись контент, иерархия блоков, доказательства
и юридическая обвязка.

## Что отличается от основной версии

**Обо мне.** Крупный портрет вместо аватарки в строке текста. Порядок:
лицо → имя → тезис → четыре доказательства → публичные профили → KERBER →
клиенты. Сетка из шести равнозначных карточек убрана: шесть одинаковых по
весу утверждений читаются как резюме, а не как основание доверять человеку
на восемь недель.

**KERBER.** Отдельный блок с логотипом-словом, ролью и одной строкой о
масштабе — над списком клиентов, а не строчкой в биографии.

**Сопровождение как основной продукт.** Первый экран и продуктовый блок
называют каналы связи (созвоны, чат, подключение к операции), а не только
количество недель. «Не курс в записи и не консультация на час» вынесено
в начало продуктового блока.

**Что убрано со страницы.** Блок «Здесь не будут учить», блок про подключение
к экрану, кейс про оплату недвижимости, блок про личные ошибки автора и блок
про переезд. Страница похудела с 18 секций до 13: между разбором программы
и предложением стояла череда остановок, каждая из которых повторяла уже
сказанное. Стили и адаптив удалённых блоков вычищены вместе с разметкой.

**Один формат вместо двух.** Самостоятельный пакет убран: рядом с личным
ведением он читался как дешёвая альтернатива тому же самому, хотя это другая
услуга. Карточка занимает всю ширину сетки, внутри разложена на две колонки:
слева описание и призыв, справа состав. Стоит она на чёрном экране с кучей
монет — на месте прежнего блока про личные ошибки. Цены на странице нет — вместо неё
призыв «Узнать подробнее»;
фиксированная сумма, порядок расчётов и правила возврата остаются в оферте.
Теги `fiat / crypto` убраны: до определения юридической схемы расчётов
криптооплата не рекламируется.

**Только личная работа.** Групповые лекции, потоки и общий чат убраны со
всего сайта и из юридических документов. Состав: 16 личных созвонов по часу,
переписка в чате все восемь недель и 5 подключений с демонстрацией экрана
в момент операции.

**Первый экран.** Заголовок — одно слово CRYPTO во всю ширину сетки. Набран
в SVG: обычным текстом кегль пришлось бы подбирать под каждую ширину экрана
и всё равно промахиваться. viewBox снят с настоящих чернил Geist 600
(`measureText` на кегле 254), поэтому буквы упираются в оба края без
растяжения. Слово белое, строка под ним на 80% белого, кнопка — искрой: единственный
цветной элемент первого экрана и единственный призыв, который человек видит
до того, как начал читать. Остальные кнопки на сайте белые; если понадобится
так же везде — это одно значение `--c-accent` в тёмной теме.

**Искажение под курсором** (порт WarpText из reactbits) — только на десктопе.
Слово рисуется в offscreen-канвас, шейдер читает эту текстуру со смещением
по фрактальному шуму, а включает смещение гауссова линза вокруг курсора:
в покое надпись стоит ровно, искажённый текст просто не читается. На тач-экране
модуль не запускается вовсе — там наводить нечем и остаётся исходный SVG,
не зависящий ни от WebGL, ни от JS.

**Переходы между экранами.** Секция прилипает к верху окна, следующая
наезжает на неё сверху. Класс вешает JS, и только там, где меняется цвет фона: приём читается как
передача цвета, а между двумя одинаково чёрными секциями видно лишь то, что
текст ни с того ни с сего замер.

Секция размером с окно прилипает верхним краем к нулю; короткую (меньше 92%
высоты) не трогаем — под ней осталась бы полоса следующей. Секция выше окна
прилипает отрицательным смещением `top = vh − h`: она замирает ровно тогда,
когда её нижний край доходит до низа окна, и дальше на месте стоит её
последний экран. Нулевой `top` там сломал бы страницу — секция замерла бы
верхним краем, и всё ниже сгиба стало бы недостижимым.

Длинные блоки прилипают только на широких экранах: смещение считается
от высоты окна, а на телефоне она меняется на ходу вместе с адресной строкой,
и посчитанное значение мгновенно устаревает. Пересчёт идёт на каждый ресайз.

Высота меряется через `getBoundingClientRect`, а не `offsetHeight`: тот
округляет до целого, и на дробной высоте нижний край прилипшей секции вставал
выше низа окна — по низу экрана проскакивала полоска того, что лежит под ней.
Смещение округляется строго вверх плюс запасной пиксель; переполнение прячется
под сгибом и попадает на собственное поле секции.
На широком экране прилипают шесть секций из тринадцати, на телефоне — только
первый экран.

Ради этого каждая секция получила собственный непрозрачный фон и слой выше
первого экрана: без фона у тёмных секций прилипший герой просвечивал бы
сквозь них. Фон задан через `background-color`, а не `background`, чтобы
не сбрасывать тени и градиентные кромки плашек.

**Графика карточек сценариев.** Четыре разных интерфейса вместо четырёх
наборов абстрактных плашек: телефон с экраном перевода, кошелёк с балансом,
лист транзакции с сетью и обрезанными адресами, облако терминов с вопросом
в середине. Логотип USDT взят из `assets/coins` — того же набора, что падает
на первом экране. Внутренности макетов заданы в `em` от одной ручки на макет:
на узкой колонке достаточно уменьшить её, и подписи с полями ужимаются
пропорционально, а не ломают строку.

**Сетка.** Шаг и толщина сняты с присланного макета, цвет переведён
в монохром: в макете сетка сиреневая, здесь любой цветной слой стал бы вторым
акцентом рядом с искрой. Маска гасит линии к центру — это не украшение,
а чистое поле под содержимым. Маска из двух слоёв пересечением: радиальная
`--grid-fade` держит чистое поле в середине, линейная `--grid-edges` гасит
линии у верхнего и нижнего края блока — без неё они упирались в границу
секции и обрывались ровной чертой, и сетка читалась как картинка с краями,
а не как фон. Шаг и цвет лежат в `--grid-lines`. Какое-то время сетка стояла и на тёмных секциях в глубине
страницы, сейчас осталась только на первом экране. Под словом — строка вполовину кегля заголовка и кнопка, больше
ничего: ни карточки автора, ни даты старта. A/B-переключатель вариантов
заголовка удалён вместе с разметкой.

**Блок про риски.** Слева заголовок и абзац, справа облако плашек. Прежняя
радиальная схема с центральным узлом и лучами требовала JS, отдельной
раскладки для телефона и полутора экранов высоты — при том что говорила
ровно то же: рисков много и они разные. Плашки идут ровными рядами: сдвиг
через одну ломал переносы — ширина у плашек разная, и шаг 3n не совпадал
с реальными границами рядов.

**Чему вы научитесь** — список сверху вниз: картинка слева, заголовок
и пояснение справа. Была лента с горизонтальной прокруткой, но она прятала
половину пунктов за краем экрана — а это единственное место на странице,
где сказано, что человек в итоге будет уметь. Графика — двенадцать
квадратных макетов в той же стилистике, что и карточки сценариев: покупка,
чек на вывод, телефон с балансом, seed-фраза с замазанными словами, выбор
сети с логотипами, сверка адреса, тестовый перевод, окно эксплорера, выбор
комиссии, запрос доступа от контракта, сеть топ-токенов и своя схема
хранения. Всё внутри задано в `em` от одной ручки на макет.

**Программа** — аккордеон, в котором всегда раскрыта ровно одна неделя.
Первая открыта прямо в разметке, поэтому её видят и поисковики, и читалки.
Повторный клик по раскрытой неделе на десктопе ничего не делает: пустой
аккордеон оставлял дыру там, где только что был текст. На телефоне закрыть
можно — там открытая неделя занимает экран.

**Куча монет** переехала в самый низ подвала: в блоке со стоимостью она лезла
на юридическую сноску, а под подвалом ей ничего не мешает.

**Марка.** Сплошной белый круг с чёрной восьмёркой. Лепной шар с бликом был
единственным трёхмерным объектом на сайте и читался как чужой элемент.

**Палитра.** Чёрный фон без фоновых градиентов и свечений, белый текст,
белые кнопки — как на kerber.agency. Единственный цвет — `#C1FF0F`: цифры
и подчёркивания в заголовках. На светлых полосах искра не может быть текстом
(контраст к белому 1.3:1), поэтому там она уходит в полосу под чёрными
буквами. Анимация с монетами оставлена без изменений.

**Юридический слой.** Оферта, политика обработки ПД, согласие на обработку ПД.
Подвал даёт четыре ссылки, четвёртая ведёт в раздел 17 оферты — там живут
реквизиты исполнителя. В подвале они занимали шесть строк на каждой странице
и спорили с дисклеймером, а нужны ровно в момент чтения условий.

## Что нужно заполнить перед публикацией

Всё незаполненное подсвечено в вёрстке классом `.fill` — розовым пунктиром,
чтобы сайт нельзя было выложить с рыбой:

- реквизиты ИП или ООО в разделе 17 оферты: ФИО, ИНН, ОГРНИП, счёт, банк, адрес, почта;
- даты редакций юридических документов;
- сроки хранения записей и данных, если они отличаются от предложенных;
- сервис видеосвязи и платёжный сервис (разделы 6 политики и 10 оферты);
- допустимые способы расчётов.

Отдельно: **фотография.** `assets/img/author-source.jpg` — 3D-рендер,
а не снимок. ТЗ требует живую фотографию; разметка и кроп 4:5 уже готовы,
нужен файл минимум 900×1125.

Юридические тексты — рабочий проект под фактический состав программы,
а не готовые документы. Перед приёмом оплат их должен вычитать юрист.

## Запуск

```bash
python3 -m http.server 4174
```

Сборки нет: обычный статический сайт, файлы отдаются как есть.

## Структура

```
index.html      главная
offer.html      публичная оферта
privacy.html    политика обработки персональных данных
consent.html    согласие на обработку персональных данных
assets/css/     tokens · styles · responsive · doc (юридические страницы)
assets/js/      main · coinpit (физика монет на canvas)
assets/img/     портрет, логотипы клиентов
```

## Кнопки призыва

Все ведут в личку в Telegram с заранее написанным сообщением. Адрес — одна
строка в начале `assets/js/main.js`. Форм на сайте нет, поэтому персональные
данные сайт не собирает: это отдельно оговорено в политике.
